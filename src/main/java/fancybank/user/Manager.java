package fancybank.user;

/**
 * @author Haodong Chen hjc5283@bu.edu
 * Class for bank manager. 
 */
public class Manager extends User {
    
    public Manager(UID UID, Name name, Address address, Email email, Password password) {
        super(UID, name, address, email, password);
    }

}
